import type { Metadata } from "next";import Image from "next/image";import Link from "next/link";import {notFound} from "next/navigation";import {ChevronRight,ExternalLink} from "lucide-react";import {accessories,formatPrice,getProductBySlug,productImage,productSlug} from "@/lib/products";
type Props={params:Promise<{slug:string}>};export function generateStaticParams(){return accessories.map(p=>({slug:productSlug(p)}))}export async function generateMetadata({params}:Props):Promise<Metadata>{const{slug}=await params;const p=getProductBySlug(slug,accessories);return p?{title:`${p.marke} Airfryer-Zubehör`,description:p.kurzbeschreibung||p.titel}:{}}
export default async function AccessoryDetail({params}:Props){const{slug}=await params;const p=getProductBySlug(slug,accessories);if(!p)notFound();return <section className="inner-section shell"><div className="breadcrumbs"><Link href="/">Start</Link><ChevronRight/><Link href="/zubehoer">Zubehör</Link><ChevronRight/><span>{p.marke}</span></div><div className="accessory-detail"><div><Image src={productImage(p)} alt={p.titel} fill priority sizes="(max-width:800px) 94vw, 50vw"/></div><article><span className="eyebrow">{p.unterkategorie}</span><h1>{p.titel}</h1><p>{p.kurzbeschreibung||"Zu diesem Zubehör liegen die aufgeführten Amazon-Produktdaten vor."}</p>{p.kapazitaetL&&<p><b>Angegebene Größe:</b> {p.kapazitaetL} Liter. Bitte prüfe die exakte Kompatibilität mit deinem Modell beim Anbieter.</p>}<strong className="accessory-price">{formatPrice(p.preisEUR)}</strong><small>Preisstand 22.08.2026</small><a href={p.affiliateUrl} target="_blank" rel="nofollow sponsored noopener" className="button affiliate-button">Preis bei Amazon prüfen <ExternalLink/></a><p className="affiliate-note">Affiliate-Link. Produktdaten und Verfügbarkeit können sich ändern.</p></article></div></section>}
