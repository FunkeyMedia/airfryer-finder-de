"use client";
import { useSearchParams } from "next/navigation";
import Image from "next/image";
import Link from "next/link";
import { Plus, Star, Trash2 } from "lucide-react";
import { useState } from "react";
import { formatNumber, formatPrice, productImage, productSlug, type Product } from "@/lib/products";
export function CompareTool({products}:{products:Product[]}){const params=useSearchParams();const initial=(params.get("ids")||"").split(",").filter(Boolean).slice(0,4);const [ids,setIds]=useState<string[]>(initial);const selected=ids.map(id=>products.find(p=>p.asin===id)).filter(Boolean) as Product[];const available=products.filter(p=>!ids.includes(p.asin));const rows:[string,(p:Product)=>string][]=[["Preis",p=>formatPrice(p.preisEUR)],["Kapazität",p=>p.kapazitaetL?`${p.kapazitaetL} L`:"Keine Angabe"],["Garzonen",p=>p.zonen?String(p.zonen):"Keine Angabe"],["Leistung",p=>p.leistungW?`${p.leistungW} W`:"Keine Angabe"],["Programme",p=>p.programme?String(p.programme):"Keine Angabe"],["Max. Temperatur",p=>p.maxTempC?`${p.maxTempC} °C`:"Keine Angabe"],["Bewertung",p=>p.bewertung?`${p.bewertung.toFixed(1)} / 5 (${formatNumber(p.anzahlBewertungen)})`:"Keine Angabe"],["Bauform",p=>p.unterkategorie]];return <section className="inner-section shell compare-wrap"><div className="compare-selectors">{selected.map(p=><div key={p.asin}><button onClick={()=>setIds(ids.filter(id=>id!==p.asin))} aria-label={`${p.marke} entfernen`}><Trash2/></button><Image src={productImage(p)} alt="" width={110} height={110}/><b>{p.marke}</b><span>{p.modellcode||p.titel.slice(0,28)}</span></div>)}{ids.length<4&&<label><Plus/><span>Gerät hinzufügen</span><select value="" onChange={e=>e.target.value&&setIds([...ids,e.target.value])}><option value="">Airfryer wählen</option>{available.map(p=><option value={p.asin} key={p.asin}>{p.marke} {p.modellcode||p.titel.slice(0,35)}</option>)}</select></label>}</div>{selected.length?<div className="comparison-table"><div className="comparison-row product-row"><b>Modell</b>{selected.map(p=><div key={p.asin}><Star fill="currentColor"/><strong>{p.marke} {p.modellcode}</strong><Link href={`/airfryer/${productSlug(p)}`}>Details ansehen</Link></div>)}</div>{rows.map(([label,get])=><div className="comparison-row" key={label}><b>{label}</b>{selected.map(p=><span key={p.asin}>{get(p)}</span>)}</div>)}</div>:<div className="empty-state"><h2>Wähle dein erstes Gerät</h2><p>Nutze das Auswahlfeld oben oder starte von einer Produktseite aus.</p></div>}<p className="note-box">Bewertungen, Preise und Verfügbarkeit stammen aus der Datenerfassung vom 22.08.2026 und können sich geändert haben.</p></section>}
